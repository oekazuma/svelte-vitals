<!--
  Widget/ has no Widget.svelte (architecture/unit-entry-file), so it isn't a unit —
  which means this parts/ directory isn't covered by any placement
  svelte-vitals.config.ts declares for "parts" (only Card/ is), so it also trips
  architecture/reserved-name-placement.
-->
<p>Widget part</p>
