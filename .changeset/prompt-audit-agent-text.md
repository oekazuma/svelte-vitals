---
'@svelte-vitals/core': patch
'svelte-vitals': patch
---

Refresh the text that reaches an agent: the `agent` reporter's preamble no longer describes warning/info findings as SEO-only and points at each finding's Accept line instead of "confirm each rule passes"; the `improve-svelte` skill's rule catalog states that it reflects the release the skill was generated from (`svelte-vitals explain --list` is the authority for the installed version) and drops its numeric cap on missed opportunities.
