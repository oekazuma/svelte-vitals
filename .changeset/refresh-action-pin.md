---
'svelte-vitals': patch
---

`ci install` and `ci upgrade` now pin `svelte-vitals-action` at its latest release (v0.12.1) instead of v0.4.0. Run `svelte-vitals ci upgrade` to move an existing workflow to the new pin.
