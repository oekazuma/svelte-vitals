---
'svelte-vitals': patch
---

Drop the "not a runtime Web Vitals reporter" clause from the registry-visible package description.
The disclaimer led with what the tool is not, which is a weak first line in a search result. The
description now states the six categories it scores and nothing else. No behavior change.
